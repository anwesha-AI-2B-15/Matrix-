#include <stdio.h>
#define R 2
#define C 3

int main() {
    int a[R][C] = {{1,2,3},{4,5,6}};
    for (int i = 0; i < C; i++) {
        for (int j = 0; j < R; j++)
            printf("%d ", a[j][i]);
        printf("\n");
    }
    return 0;
}
